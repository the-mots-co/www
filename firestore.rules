rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Next.js API routes (Firebase Admin SDK)
    // Direct client access is denied
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
